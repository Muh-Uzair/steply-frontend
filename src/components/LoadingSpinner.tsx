import { LoaderCircle } from "lucide-react";
import React from "react";

const LoadingSpinner: React.FC = () => {
  // VARS

  // FUNCTIONS

  // JSX
  return <LoaderCircle className="text-primary h-8 w-8 animate-spin" />;
};

export default LoadingSpinner;
