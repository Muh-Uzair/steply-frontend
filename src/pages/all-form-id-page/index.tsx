import AllFormId from "@/features/all-form-id/AllFormId";
import React from "react";

interface Props {
  id: string;
}

const index: React.FC<Props> = ({ id }) => {
  // VARS

  // FUNCTIONS

  // JSX
  return <AllFormId id={id} />;
};

export default index;
